export default defineNuxtRouteMiddleware((_to, _from) => {
  // Placeholder global middleware. Add auth/permissions or analytics here when needed.
  // Currently a no-op to avoid interfering with navigation.
})


